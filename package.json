{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "glpk.js": "^4.0.1"
  }
}
